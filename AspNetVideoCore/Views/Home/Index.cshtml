
@model IEnumerable<VideoViewModel>

@{ ViewBag.Title = "Home"; }


@*foreach loop that renders the partial view.*@

@foreach (var video in Model)
{
    @Html.Partial("_Video", video);
}
<div>
    <a asp-action="Create">Create</a>
</div>